{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "static_linking_library",
            "type": "shell",
            "command": "make run",
            "options": {
                "cwd": "${workspaceRoot}/static_linking_library"
            }
        }
    ]
}