{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "c_dynamic_or_static_linking_library",
            "type": "shell",
            "command": "make dll",
            "options": {
                "cwd": "${workspaceRoot}/c_dynamic_or_static_linking_library"
            }
        }
    ]
}